# 更新紀錄

TC8 分會平台的變更紀錄。新的在上面。
每一條寫「改了什麼、為什麼」，不寫程式細節——這份是給幹部看的。

---

## 2026-08-29

### 夥伴活動：不設審核關卡，改用揭露＋事後下架

拍板不做幹部核准。**誰張貼就是誰主辦**，開放夥伴自由發揮，讓大家的商務本體有互動空間。
理由：一旦幹部要核定，「上線」就等於分會背書，責任會從主辦人轉到幹部身上；
而且第一個案例（會員的銷講）幹部就沒共識，沒有標準的審核權實務上會變成看交情。

三條規則不是寫在說明文字裡，是**做成欄位與擋關**，不照做就送不出去：

1. **費用必填** — 新增費用欄（夥伴活動本來沒有），要求寫出「對外價格／TC8 專屬價」。
   免費的活動要寫「免費」。
2. **必勾同意** — 表單頂端三條須知，不勾不能發布。
3. **日期必填** — 沒有日期就不知道什麼時候該收起來。

責任揭露放三層，因為有「全部」分頁、首頁也混著出現，只放一個地方會漏：

- **卡片**：「夥伴主辦」徽章＋主辦人姓名（跟著活動走到任何一頁）
- **分頁**：「夥伴的活動」頁常駐說明條
- **詳情頁**：底部完整聲明（按下報名連結前的最後一眼）

### 活動結束隔天自動下架

表單上原本就寫著「活動結束隔天會自動收起來」，**但程式沒做**——只把過期的排到後面，
沒有收起來。這次補上，只收夥伴活動，TC8／CXO 的官方活動照舊留著。
主辦人自己在「我的張貼」還是找得到，可以改、可以刪。

### 月例會詳情頁補回日期與時間

點進月例會看不到時間，是程式漏洞不是沒填。
日期時間存在 `event_at`，但詳情頁只讀另一組欄位，對月例會一律算成「沒有日期」，
整塊直接不顯示。首頁卡片有退路所以看得到，詳情頁漏了這條退路。

### 地點可以直接點開 Google 地圖

全站四個地方都通了：活動詳情頁、活動卡片、首頁大橫幅、報名頁抬頭。

- 只寫地名 → 用地名去 Google 地圖搜尋
- 地名後面接地圖網址 → 直接開那個地圖，畫面上只顯示地名
- 線上／遠距／Zoom／Meet／直播 → 顯示 `💻`，不接地圖（搜尋「線上課」沒有意義）

沒有另外開「地圖網址」欄位——那要改資料庫，夥伴也得多填一次。

### 幹部看得到「公開／隱藏」膠囊

活動卡片左上角、首頁大橫幅左上角、公告與問卷的標題前，
顯示 `● 公開`（綠）或 `● 隱藏`（灰），不用一則一則點進去確認。
**只有幹部看得到**：一般夥伴的資料裡根本沒有這個欄位，畫給他們看會全部顯示成「公開」。

### 上線腳本：探針結束一定把 8991 收乾淨

上線前的渲染檢查會開一個本機 server，**失敗時沒收掉**，
它會一直占著 8991 並指向已被刪掉的暫存夾，導致之後每次上線都卡住。
症狀是「頁面檢查不過」，看起來像程式壞了，其實完全無關。

---

## 已知待辦

### 🔴 來賓名額的語意是錯的

現在的「來賓名額」是**每位夥伴可以帶幾位**，但實際需求是**全場來賓總額**（暫定 10 位）。

**不能只改前端文案**：前端拿不到「全場已登記幾位來賓」這個數字，
`v_post_seats` 只回報名人數，來賓那邊只看得到「我自己登記的」。
只改文字會變成畫面上像有總量管控、實際仍按每人算——比現在更危險。

要真的做到，得改 Supabase（不在這個 repo）：
1. `v_post_seats` 加上全場來賓數
2. `guests` Edge Function 的驗證從「每人上限」改成「全場總額」

### 其他

- 單日活動的日期顯示成「到 09/20」，語意不清（`deadline` 原本是「截止日」的意思）。
- 夥伴勾選「同意三條須知」沒有存進資料庫，所以事後查不到誰同意過。
- 「跟哪位幹部提過這場活動」還沒做成欄位，目前靠線下打招呼，沒有紀錄。
